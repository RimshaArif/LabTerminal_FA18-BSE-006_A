extends ../layout

block content
  h1 Login
  .container
    form(action="/login",method="post")        
            .form-group
                label(for="email") Email
                input#email(type="email", class="form-control", name="email" value="rimshaarif723@gmail.com" required style = "border-bottom: 2px solid #343a40")
            .form-group
                label(for="password") Password
                input#price(type="password", class="form-control", name="password", value="123" required style = "border-bottom: 2px solid #343a40")
            input(type="submit", class="btn btn-info" value="Login")
